export const SETTINGS = {
    MOBILE_BREAKPOINT: 375, //actually, the style guide says that the design was done with 375px width, not that it should be the breakpoint...
    MIN_SYMBOL_HOLDER: 180, //min width
    MAX_SYMBOL_HOLDER: 650, //max width
	VERTEX_HACK: true, //whether to conform vertexFinder's results to how the design looks
	VERTEX_PRECISION: 2, //number of decimal digits in vertexFinder's coordinates
};